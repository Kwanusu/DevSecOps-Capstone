{
  "title": "Capstone Project Overview",
  "panels": [
    {
      "type": "stat",
      "title": "Requests Total (pod)",
      "targets": [
        {
          "expr": "sum(rate(container_cpu_usage_seconds_total{pod=~\"capstone-app.*\"}[5m]))"
        }
      ]
    },
    {
      "type": "graph",
      "title": "CPU (container)",
      "targets": [
        {
          "expr": "sum(rate(container_cpu_usage_seconds_total{pod=~\"capstone-app.*\"}[5m])) by (pod)"
        }
      ]
    },
    {
      "type": "graph",
      "title": "Memory (container)",
      "targets": [
        {
          "expr": "sum(container_memory_working_set_bytes{pod=~\"capstone-app.*\"}) by (pod)"
        }
      ]
    }
  ],
  "timezone": "browser",
  "schemaVersion": 38,
  "version": 1
}